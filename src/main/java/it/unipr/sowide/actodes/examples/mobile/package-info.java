/**
 *
 * Shows how actors can move between different actor spaces.
 *
 * The execution must involve more than an actor space.
 *
 * This class allows to a user to select the number of nodes
 * of the distributed application.
 *
 * Note that the first node must be a broker actor space,
 * then there can be zero or more node actor space,
 * and finally the last one must be the initiator actor space.
 *
**/
package it.unipr.sowide.actodes.examples.mobile;
