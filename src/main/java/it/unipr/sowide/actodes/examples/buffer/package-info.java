/**
 *
 * Shows how actors can implement the classical bounded buffer example
 * through an implementation based on behavior change and one based
 * on state change.
 *
 * The execution of both the two implementations is started by
 * the {@code main} method contained in the {@code Initiator} class.
 *
**/
package it.unipr.sowide.actodes.examples.buffer;
